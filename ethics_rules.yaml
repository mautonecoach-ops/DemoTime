require_care_network_if_low_trust : 55
forbid_negative_resources: true
min_resource_per_node: 1.5
min_avg_trust: 55
max_resource_concentration: 0.50
# --- LINT (pre-ejecución)
require_care_action_if_low_trust: 60   # exige al menos un care_network si confianza < 60
max_redistribute_fraction: 0.35        # fracción máxima heurística por step
# Política de bloqueo del lint
fail_on_lint: true                     # bloquea si hay violaciones (por defecto)
require_initiative_if_low_trust: 55     # NUEVO: exige al menos un launch_initiative si hay baja confianza
min_strengthen_actions_if_low_trust: 1  # NUEVO: mínimo de strengthen_ties si hay baja confianza
# --- ETHICS (post-ejecución) ---
min_trust: 60
min_cohesion: 50
min_equity: 60
max_equity_drop_pct_per_step: 15        # NUEVO: alerta si cae la equidad > X% en un paso (post-ejec)